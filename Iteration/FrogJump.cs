using System.Linq;

namespace Iteration
{
    public class FrogJump
    {
        public int solution(int[] X)
        {
            int right, sum = 100;
            var allNumbers = Enumerable.Range(0, 100000);
            var result = allNumbers.Except(X);


            return 0;
        }
    }
}